#ifndef CONFIG_H
#define CONFIG_H


// Mặc định (giá trị mô phỏng)
#define DEFAULT_MIN_MOISTURE 35 // % độ ẩm đất để bắt đầu tưới
#define DEFAULT_MAX_MOISTURE 65 // % độ ẩm đất để dừng tưới
#define DEFAULT_MAX_WATER_SEC 30 // thời gian tưới tối đa (giây) khi mô phỏng
#define DEFAULT_SENSOR_INTERVAL_SEC 5 // chu kỳ kiểm tra cảm biến (giây) - mô phỏng
#define MANUAL_WATER_SEC 10 // thời gian tưới khi bấm nút thủ công


#endif // CONFIG_H